import React from 'react';

import './styles.scss';

const Form = () => {
	return (
		<form className="user-search-form" action="#">
			<input type="text" />
			<button type="submit">Search</button>
		</form>
	);
};

export default Form;
